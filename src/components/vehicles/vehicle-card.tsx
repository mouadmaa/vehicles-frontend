import { FC } from 'react'
import { StyledVehicleCard } from '@/styles/components/vehicles/vehicle-card'

const VehicleCard: FC = () => {
  return <StyledVehicleCard></StyledVehicleCard>
}

export default VehicleCard
